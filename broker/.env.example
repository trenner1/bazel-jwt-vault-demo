# Broker config
ISSUER=http://localhost:8080
AUDIENCE=vault-broker

# Paths to JWKS + private key (relative to broker/)
KEYSET_PATH=jwks.json
SIGNER_KEYS_PATH=signer_keys.json

# Vault (your local Docker Vault)
VAULT_ADDR=http://127.0.0.1:8200
VAULT_TOKEN=root
VAULT_JWT_ROLE=bazel-builds
